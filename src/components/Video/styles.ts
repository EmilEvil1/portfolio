import styled from 'styled-components'

export const VideoContainer = styled.video`
  position: absolute;
  right: 0;
  bottom: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: -10;
`
